<?xml version="1.0" encoding="UTF-8"?>
<Context crossContext="true">
	<!-- :TODO This is an attempt to cut down on some of the configuration of ssp. 
		Need to follow through, and either remove this file (just use env var) or 
		use it and remove the env var. -->
	<Parameter name="SSP_CONFIGDIR" value="" />
</Context>
